import { CreateAppNgModuleRefPipe } from './create-app-ng-module-ref.pipe';

describe('CreateAppNgModuleRefPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateAppNgModuleRefPipe();
    expect(pipe).toBeTruthy();
  });
});
