import { AtomDirective } from './atom.directive';

describe('SelectDirective', () => {
  it('should create an instance', () => {
    const directive = new AtomDirective();
    expect(directive).toBeTruthy();
  });
});
