import { AtomFactoryDirective } from './atom-factory.directive';

describe('AtomFactoryDirective', () => {
  it('should create an instance', () => {
    const directive = new AtomFactoryDirective();
    expect(directive).toBeTruthy();
  });
});
